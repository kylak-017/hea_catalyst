# LAMMPS Input File
# Licensed under CC BY 4.0
# A Set of Tutorials for the LAMMPS Simulation Package (LiveCoMS, 2025)
# By Simon Gravelle, Cecilia M. S. Alvares, Jacob R. Gissinger, and Axel Kohlmeyer
# Please cite doi.org/10.33011/livecoms.6.1.3037
# Find more on GitHub: https://github.com/lammpstutorials

# PART A - ENERGY MINIMIZATION
# 1) Initialization
units lj # reduced dimensionless; most useful for stat mech calculations
dimension 3 # 3D space
atom_style atomic # point masses without charges 
boundary p p p # periodic, no vaccum or random extending space (bounded)
# 2) System definition
region simbox block -20 20 -20 20 -20 20 # simbox dimensoiopn 20 by 20 by 20 on all axes
create_box 2 simbox  # reserves space for 2 types of atoms
create_atoms 1 random 1500 34134 simbox overlap 0.3 # adds 1500 atoms of tyoe A,  min distabce 0.3 between different A's
create_atoms 2 random 100 12756 simbox overlap 0.3

# 3) Settings
mass 1 1.0 #mass of type 1 atom
mass 2 5.0
pair_style lj/cut 4.0 # lenard jones potential style cutoff of 4 units
pair_coeff 1 1 1.0 1.0 # where (atom type 1, atom type 1) epsilon (depth of potential wall), sigma (distance of interactioinn strength)
pair_coeff 2 2 0.5 3.0 

# can use pair_modify to change interatomic (different atomic types) interaction definitions 
# 4) Monitoring
thermo 10 # every 10 steps update 
thermo_style custom step etotal press # -outputs to print

# 5) Run
# run 0 post no ;;; no time propagatioj\
minimize 1.0e-6 1.0e-6 1000 10000 # mininize the energy so that overlapping atoms are removed (repulsive ebergy is ridden of)

# PART B - MOLECULAR DYNAMICS
# 4) Monitoring
thermo 50 # update 50 times
thermo_style custom step temp etotal pe ke press # non zero temp, (pe, ke) should be monitored
#dump 1 all atom 1000 dump.atom # Dumps atomic info every 1000 timesteps
dump viz all image 100 myimage-*.ppm type type size 800 800 zoom 1.452 shiny 0.7 fsaa yes &
    view 80 10 box yes 0.025 axes no 0.0 0.0 center s 0.483725 0.510373 0.510373
dump_modify viz pad 9 boxcolor royalblue backcolor white adiam 1 1.6 adiam 2 4.8
# 5) Run
fix mynve all nve # move atoms aCCORDING to newton velocity verlet algorithm --> shown more in blog
# NVE = microcanonical ensemble, N is kept constant
fix mylgv all langevin 1.0 1.0 0.1 10917 #langevin thermostat
timestep 0.005 # each step take 0.005 time units
run 50000 # 50000 times ran

